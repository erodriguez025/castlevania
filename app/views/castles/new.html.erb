<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1>Create a new castle</h1>

      <%= simple_form_for @castle do |f| %>
        <%= f.input :name %>
        <%= f.button :submit, class: 'btn btn-primary' %>
        <%= f.input :photo, as: :file %>
      <% end %>
    </div>
  </div>
</div>